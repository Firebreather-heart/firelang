expr : term((PLUS|MINUS) term)*

term : factor((MUL|DIV) factor)*

factor : INT|FLOAT
       : (PLUS|MINUS) factor
       :LPAREN expr RPAREN

if expr {
       
}
for i = start; i = stop ; step x {
       expr
}
while cond {
       expr
}